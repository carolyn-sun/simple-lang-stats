# Environment Variables Reference
# 
# ⚠️  IMPORTANT: Cloudflare Workers do NOT use .env files!
# This file is for reference only. Use wrangler.toml or wrangler secrets instead.
#
# For local development, configure in wrangler.toml:
# [env.development.vars]
# GITHUB_TOKEN = "your_token"
# ENABLE_PRIVATE_ACCESS = "true"
#
# For production, use wrangler secrets (recommended):
# wrangler secret put GITHUB_TOKEN
# wrangler secret put ENABLE_PRIVATE_ACCESS

# GitHub Personal Access Token (optional)
# Used to improve GitHub API rate limits and access private repositories
# 
# How to create: GitHub Settings > Developer settings > Personal access tokens > Tokens (classic)
# 
# Required permissions:
# - For PUBLIC repositories only: No scopes required (or 'public_repo' for write access)
# - For PRIVATE repositories: 'repo' scope (Full control of private repositories)
#
# ⚠️  Configure this in wrangler.toml or as a secret, NOT in .env files
GITHUB_TOKEN=your_github_token_here

# Enable private repository access (true/false)
# When set to true, users can access private repos by providing their own token
# ⚠️  Configure this in wrangler.toml or as a secret
ENABLE_PRIVATE_ACCESS=true

# API Secret (optional, for additional security)
# ⚠️  Configure this as a secret for production: wrangler secret put API_SECRET
API_SECRET=your_api_secret_here